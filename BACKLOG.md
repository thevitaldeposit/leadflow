# Stream — Master Backlog

> Source: project handoff + build sessions + repo TODO/notes harvest. Deduplicated and triaged. `file:line` refs included where known. Living source of truth — update on every build.

---

## 0. Done (recent)
- [x] **The drop/pickup task is a real screen, launched from BOTH schedule surfaces — plus the picker settle and the delete-release.** Four things, one build. **(1) One shared task summary.** The per-job payload the calendar built inline (`schedule.js:105-146` — phone, address, size, `assignedUnits`, `dropRecorded`/`pickupSettled`/`hasAssignments`, `pendingSwapOuts`, dump tickets, `overageNeedsRate`) is now `services/scheduleTaskService.js` and is served three ways: embedded per day by `/api/schedule/calendar` (unchanged shape — verified the calendar's row is `===` the service's own summary), one job at `GET /api/schedule/task/:leadId`, and a batch at `GET /api/schedule/tasks?ids=…`. The dashboard's Today's Schedule was reading a plain `getLeads` payload and structurally could not render any of it. **(2) The task is its own route**, `/task/:leadId?type=delivery|pickup|active` (`pages/JobTaskPage.jsx`), registered **outside** the dashboard chrome so it's genuinely full-bleed on a phone and a `max-w-2xl` centered screen on desktop, with ONE scroll — the page's. It replaces `JobTaskModal.jsx` (deleted): a ~450px sheet with a sticky header, a sticky footer and three nested inner scrollers at 11px type, which is what made the task read as a cramped panel. Same steps, same components, same endpoints: delivery → confirm the unit; pickup → unit → **dump-site chooser** (+ directions) → weight (always-visible lbs box + camera); active → record the swap replacement. Call/Navigate sit in the header (drop Navigate → the customer's address; the dump-site step keeps its own directions link). **(3) Both surfaces launch it** — the Schedule day card and the dashboard's Today's Schedule row both `navigate('/task/:id?type=…')` instead of `/leads/:id`, and both keep a small explicit ↗ affordance to the customer profile. Today's Schedule now also shows the on-site unit and greys out / Done-chips a finished drop or pickup, matching the Schedule page. **(4) The drop picker settles.** `UnitDropAction` used to re-fetch the fleet after a successful drop and stay open; since the unit just dropped is no longer assignable, a job whose last free matching unit had gone out fell through `showingAll = … || matching.length === 0` and repainted with the **entire fleet** under "which unit is going on the ground?". The step now settles to "Unit 12 is on the ground ✓" and renders no picker at all; the all-sizes list is reachable **only** through the explicit "Different size (substitute)" choice (with its own empty state: "no 20 yard is free" ≠ "the whole fleet is out"); and a delivery whose `dropRecorded` is already true opens as **done**, not as a picker (the swap-replacement task, which deliberately drops a second unit, is never treated as finished). **(5) Deleting a job or customer hands its dumpster back.** `assignmentService.releaseUnitsForLead` closes a job's OPEN assignments and returns those assets to `available`; it's called by `DELETE /api/leads/:id` (before the row and its cascading assignments vanish) and by `softDeleteCustomer`. Before this, binning a customer left the assignment open for the full 30 days, so the drop picker kept hiding a can that had nowhere to be while the date-window availability — which counts jobs, not units — had already freed it. Picked-up/weighed assignments stay as history, and the yard queue is not polluted (it excludes binned jobs). **Nothing verified was changed**: per-unit attribution, `units_out`/completion, the swap markers and dedup, the availability overlap math, and the Twilio/extraction paths are all untouched. Verified **77 checks** on a temp DB against the real migrations, services and routers: summary shape + exact key set + tenant isolation + binned-job 404, state through drop → pickup → yard queue, the calendar row identical to the shared summary, the `/task` and `/tasks` endpoints incl. auth, unknown/junk ids and cross-tenant reads, the legacy no-assignment fallback to `delivery_done_at`, and the release path — unit freed and re-assignable after both a lead delete and a customer bin, idempotent, other jobs' units untouched, history preserved, and **availability byte-identical across a release** (3 → 2 overlapping jobs on the bin, unmoved by freeing a unit).
- [x] **Guided drop/pickup flow — the schedule card is now the task, not a link.** Tapping a day card used to `navigate('/leads/:id')` and leave the driver to find the right button among several scattered actions. It now opens a **card-launched guided modal** (`components/home_services/JobTaskModal.jsx`, phone-first bottom sheet `items-end sm:items-center`) that walks the one task in the order it happens on the ground; the customer profile is a small explicit affordance on the card and in the modal header. The modal **owns no logic** — it sequences the existing prop-driven components unchanged (`UnitDropAction`, `UnitPickupStep`, `DumpTicketAction` compact) against the existing endpoints. **Delivery** → header (customer / size / address) + Call / Navigate → confirm the drop, unit number required. **Pickup** → header + Call / Navigate → which unit came back (required) → **which dump site** (+ Get directions) → the weight; **"Picked up — weigh later"** finishes there and the can waits in the yard queue. **Active rental** → the same header + record the swap replacement (it lands mid-rental, on neither the delivery nor the pickup day). **Dump sites** are new: `dump_sites` table + `dumpSiteService` + `/api/dump-sites` CRUD + a **Dump Sites** page in the Home Services nav (name, address, notes, retire/restore as a soft delete so an old ticket that names a site still reads). Directions are an address-only maps deep-link — no geocoding, no distance, no effect on the mileage fee. **Weight step**: the lbs box is **always visible and always typeable**, with a **camera icon beside it** (`capture="environment"`) → `POST /api/scale-tickets/read` (new multer route) → `services/scaleTicketReader.js`, a **new file** that COPIES the base64-image → Claude pattern (`claude-sonnet-4-6`) rather than touching the fenced `extractionEngine.js`. It returns `{ weightLbs, label, confidence }` — the pounds AND **which line it read** (tickets print gross/tare/net, sometimes in tons; it converts and prefers net) — and **pre-fills** the box for the owner to confirm or overwrite. It **never auto-submits**, and a failed/unconfigured read still returns 200 with the stored photo so manual entry is unaffected. Ticket photos are written under **`RECORDINGS_DIR/scale-tickets/`** — the same persistent Railway volume as the call recordings, served at `/uploads/recordings/...` — NOT bare `/uploads`, which is wiped on redeploy; the photo rides on the ticket as overage evidence (`photoPath`) alongside the chosen `dumpSite`, and a weight **correction preserves both**. **Task done clears today's schedule, read-time only**: `schedule.js` reads each job's assignments **without** the `picked_up_at IS NULL` filter (`assignmentTaskStateForLeads`) and returns `dropRecorded` / `pickupSettled` (**rows exist AND all picked up**) — a settled task greys out with a Done chip, sorts to the bottom, and stops putting a dot on the day cell. The **"rows exist" clause is the guard**: an empty `every()` is vacuously true, so zero assignments derives *nothing* and a legacy job stays visible; those fall back to new nullable `delivery_done_at` / `pickup_done_at` columns stamped by `POST /leads/:id/task-done`, which **refuses (400 `has_assignments`) any job that does track units** and writes nothing but the timestamp. `units_out`, the completion gate, the swap markers, `recordDumpTicket`'s decisions and availability are untouched. The scattered profile actions are gone (`CustomerDetailPage` inline weight entry + "Record the unit on site"); the **yard queue stays** as the later-weighing surface. Verified **122 checks** on a temp DB against the real migrations, services and routers: dump-site CRUD + tenant isolation + soft delete; derived task state for legacy / dropped / settled / two-unit-partial jobs; the legacy fallback's guard and idempotency; the calendar's flags before and after a real pickup; photo + site stored and preserved through a correction while every billing field matches a no-photo control exactly; junk `photoPath` / nameless site dropped; the upload lands on the volume path and a failed read keeps the photo; and the Phase 2 swap suite re-run green (paid swap holds through haul 1 and completes on the final pickup, manual swap still holds a unit, legacy by-lead path unchanged, yard queue unchanged). **Two real bugs caught and fixed while verifying**: `Number(null)` is `0` and finite, so a new dump site pinned to `sort_order` 0 and an *unreadable* ticket pre-filled the weight box with a confident-looking **0 lbs**. **Not verified locally**: the live vision round-trip (no `ANTHROPIC_API_KEY` on this machine) — the reader's parse/normalize layer and the route's failure path are covered, the model call itself runs first in Railway.
- [x] **No email-send path can silently send to nobody — payment link + invoice require a deliverable address.** Choosing **Send Payment Link** with no customer email used to proceed silently: the job moved to `pending_payment`, the timeline claimed "payment link emailed", and `sendInvoiceLinkEmail` no-op'd with `{sent:false, reason:'no_email'}` in a server log the owner never sees. The email-a-link decision is now **gated at the server**, where it's actually made — `POST /leads/manual` (unpaid booking) and the `PUT /leads/:id` booking reroute both 400 (`code:'email_required'`) **before anything is written**: no lead, no customer, no invoice, no activity row, no status change. Validity is one shared test (`emailService.isValidEmail`, mirrored client-side in `utils/email.js`), and the address checked is the one the invoice would bill — the typed/lead email, else the linked customer's, else the customer the phone reconciles to — so a call lead whose customer has an email on file is never falsely blocked. **Clients**: the manual form disables Send Payment Link with the reason + an "Add an email" jump back to step 1 (and shows Email in the booking summary); the customer-profile **Create Job** modal does the same off the real customer email; the two **Confirm Booking** modals (lead detail + dashboard) no longer swallow the refusal — they stay open and print it, as does the lead-detail status dropdown. **Invoices**: `POST /invoices/:id/send` takes `requireEmail` (sent by the review screen's **Approve & Send**, implied by `channel:'email'`) and refuses **before `markSent`**, so a draft is never stamped sent with nothing delivered; Approve & Send is disabled without a valid Bill To email and now also reports a provider-level delivery failure instead of navigating away. `InvoiceDetailPage`'s Send was already honest ("email failed (no address)" + no-email/no-phone hints) and is unchanged. The **system-driven** sends (call auto-book, weight/overage + swap bills) can't be blocked — nobody is watching — so they now write "Invoice N could NOT be emailed — no email address on file" to the job timeline (`jobLifecycle.emailInvoiceLink`) instead of only a console line. **Mark Paid and Save-as-inquiry are untouched** (they email nothing and are never gated), as is the send mechanism itself. Verified **27 checks** against the real routers on a temp DB: blocked-with-nothing-created for blank + malformed emails, allowed with a valid one, allowed via the linked customer's email, Mark Paid / inquiry / plain edits unaffected, invoice send blocked without marking sent, and plain `channel:'both'` behavior unchanged.
- [x] **Pickup/asset Phase 2 Build B / 2d — configurable swap weight allowance. Phase 2 is COMPLETE.** Every haul used to get a fresh full allowance, so a swap silently doubled what the customer could dump. New per-business setting `swapWeightAllowance` — **`full` (default, today's behavior exactly)** or **`none`** (the replacement's load bills from ton zero, for operators whose swap fee already covers that dump) — edited on the **Pricing page** in its own "Swap Weight Allowance" card, right under the per-size Swap-out pricing, saved through the existing `/api/settings` round-trip. **Detection**: `recordDumpTicket` calls the new `isReplacementHaul()`, which requires BOTH that the job actually had a swap (`vd.swapOutsApplied`, an earlier `swap`/`swapOut` ticket, or a live billed swap line) AND that this ticket's assignment is **not the job's first** unit — so the swap-OUT haul (the original can coming back) and a delivery-time substitution on a job that never swapped both keep the full allowance. The decision is persisted as `replacementHaul` on the ticket next to `assignmentId`/`size`, and `updateDumpTicketWeight` re-prices from that stored flag, so a correction can't drift onto the other basis. **One boundary**: the toggle is applied only inside `computeOverage({ size, replacementHaul })` (zeroing `includedTons`); `getOverageConfig` / `pricingService.getSizeWeightConfig` stay pure per-size resolvers. Completion, `units_out`, the swap markers and the swap-billing dedup are untouched. Also: the public invoice's "this rental includes N tons" disclosure is suppressed on a bill that was raised with zero allowance (`invoiceService.billedWithoutAllowance`), so a `none` invoice can't contradict its own "over 0 included" line. Verified **57 checks** (42 service + 15 HTTP): original haul always full allowance; replacement haul $100 under `full` vs $300 under `none` on the same 3-ton weight; correction holds the stored basis; legacy no-assignment jobs, the two-can/substitution job and the manual-swap checkbox path all unchanged; per-tenant isolation of the setting; a size-changing swap still prices on the replacement's own size (2c); **Fix A still passes** — the paid swap keeps the job open through the first haul and completes on the final pickup, on both the assignment and the legacy path. Not built (deliberate, prompt scope): a *custom* per-swap allowance amount — only full/none.
- [x] **Pickup/asset Phase 2 Build A — reliable swap drop capture + authoritative swap marker.** Closes the two swap-shaped gaps that made the universal pickup flow skip swaps; **prerequisite for 2d** (it does not add the allowance config). **(1) The marker is now authoritative.** In `jobLifecycle.recordDumpTicket` the `pendingSwapOuts` branch is checked **before** the manual `swap` checkbox, so an outstanding PAID swap is consumed by that haul whether or not the owner also ticked the box — the box used to short-circuit the branch, leave the marker armed and silently demand a **phantom third ticket** before the job could complete. Ticking it on a paid swap now completes in **two** tickets. The genuine manual swap (no paid swap invoice) is unchanged: it still holds the unit out and still bills the replacement at the size's swap price. **Same-class bug found and fixed while proving it:** paying the **dump ticket's own** $175 swap bill re-armed `pendingSwapOuts` for a rotation that had already happened (`applySwapOutOnPayment`), so a manual swap ALSO needed a third ticket — verified A/B against pre-change code as **pre-existing, not a regression**; the hook now skips any invoice raised by a swap ticket. **(2) The replacement drop is capturable where it actually happens.** `dropUnit`'s only entry point was the schedule's *delivery-day* card, but a swap replacement lands mid-rental — so it never became an assignment and could never be picked up, queued at the yard, or weighed as itself. Now: the weight form **prompts for the replacement's unit number** the moment the server reports a swap-out haul (`swapOut` on the dump-ticket response, straight into the existing `UnitDropAction`), the schedule's **active-rental** cards carry the same Call / Navigate / **Record replacement** row, and the customer profile has a "Record a replacement unit" action for a live job; the yard queue names the job to open when a swap-out is weighed days later. `pendingSwapOuts` is surfaced on the calendar + customer payloads so the weight form hides the now-redundant checkbox and explains why. Completion stays **purely `units_out` reaching 0** — the gate itself is untouched, assignments remain location state, and the swap-billing dedup (`swapPreBilled` / `swapAlreadyBilled`) is preserved. Verified **76 checks** (service + HTTP), plus the full 2b (68) and 2c (121) suites re-run green — **265 total**: Fix A still keeps the job open through the first haul and completes on the final pickup (with and without units), checkbox-on-a-paid-swap completes in two tickets, manual-swap-without-a-paid-invoice still holds the unit and bills $175, and a replacement dropped on an active rental flows drop → pickup → yard queue → weigh → completion with its overage priced on its own size. **2d (configurable swap weight allowance) remains.**
- [x] **Pickup/asset Phase 2c — weight + overage attribute to the unit's job.** A weight now belongs to an **assignment** (unit + job), not "the lead I opened". `POST /api/leads/:id/dump-ticket` takes an optional `assignmentId`; `jobLifecycle.recordDumpTicket` resolves it first and everything after runs on **that assignment's job**: the overage prices against the size read off the **unit's asset** (so a size-changing swap bills the right allowance/rate), the ticket + invoice land on that lead/customer, and `units_out` / completion advance there — which is what fixes the **two-concurrent-jobs** bug (entering one job's ticket no longer completes another). The ticket records `size` / `assignmentId` / `assetId` / `unitLabel`, so a later weight correction re-prices against the size **that ticket** was written for. On save the assignment is stamped `weighed_at` and the can returns to `available`; a can can only be weighed once (409 — corrections go through the existing PATCH). **Yard queue** (`GET /api/assets/yard`, `components/home_services/YardQueue.jsx`, top of the Schedule page, hidden when empty): every can picked up but not yet weighed, oldest first, each with its own job — the weekend case, four cans collected Saturday and weighed over the following days, each landing on the right customer. The schedule's pickup card threads the just-picked assignment into the weight entry, and the customer profile offers the job's on-site can(s) — both resolve down the same assignment→job path. **Size write-back**: dropping a can of a different size than the job records now rewrites `vd.dumpsterSize` (preserving `dumpsterSizeOriginal`), so availability, pricing and display stop resolving against a size that isn't there; a non-size-shaped asset label can never clobber a good job size. Legacy jobs with no assignment fall through to the exact pre-2c by-lead path. Migration `assignments_weighed_at_backfill_2c` (new `migration_flags` table) closes pre-2c pickups so the queue doesn't open full of already-handled cans. Swap markers (`pendingSwapOuts` / `swapOutsApplied`), `units_out` accounting and the completion **gate** are unchanged in how they decide anything — only which job drives them. Verified **121 checks** (89 service + 32 HTTP): Fix A's paid-swap flow still keeps the job open through the first haul and completes on the final pickup **both** with and without units; two concurrent jobs complete independently; tenant isolation on the queue and the weigh action; availability byte-identical across a full drop→pickup→weigh cycle. **2d (configurable swap weight allowance) and the swap-checkbox double-arm remain.**
- [x] **Pickup/asset Phase 2b — drop/pickup capture the actual unit.** The `assignments` table created empty in 2a is now wired by `services/assignmentService.js` (`GET /api/leads/:id/units`, `POST /api/leads/:id/units/drop`, `POST /api/leads/:id/units/pickup`, all `requireAuth`). **Drop** (new action on the schedule's *delivery* card, mirroring Phase 1's pickup card — Call / Navigate / **Record drop**): the driver **must** pick a unit number — free units of the job's size first, "Different size (substitute)" opens the rest of the free fleet — which creates the assignment (`dropped_at = now`) and sets the asset `out`. The same action drops a **swap replacement** (a job can hold several open assignments at once). **Pickup**: the Phase 1 card now **requires** choosing which on-site unit came back (only that job's open assignments are offered; auto-selected when there's one), stamping `picked_up_at` and setting the asset `at_yard`; the weight / dump-ticket entry that follows is **byte-identical to before** (verified: same `unitsOut`, same `advancedTo`, same ticket shape with and without an assignment) and a job delivered before capture existed falls straight through to it. Validation: a unit can be out on **only one job at a time** (409, names the other job), a retired / out-of-service / other-tenant unit is refused, and pickup only accepts a unit actually on site. `at_yard` units are re-assignable so the yard→job→yard loop closes without a manual status flip. Assigned unit numbers show on the schedule day cards and in the customer profile's Job Details grid (read off the **booked** lead, so a later follow-up call doesn't blank it). Availability/fleet math, `units_out`, swap markers, `recordDumpTicket`, and the completion gate are untouched — verified 68 checks (service, HTTP routes, customer payload), including availability byte-identical before/after a full drop→pickup→re-drop cycle. **2c (per-unit overage attribution, swap allowance, size write-back, checkbox double-arm) remains.**
- [x] Call-intent classifier: Part 1 + 1b (swap+extension together) + Part 2 (editable review → approve → send → sign → pay)
- [x] Swap Fix A — paid swap keeps job open until replacement pickup (no premature completion / orphaned inquiry)
- [x] Swap Fix B — correct swap duration (tz-safe day count) + editable swap delivery date on review screen
- [x] Review-flow UX — review item now inserts into the Action Queue **live** (upsert on `lead_updated`, no manual refresh); rows show the linked customer's **real name** (server-resolved via `customer_id`, plain lookup); in-row **Review** is the obvious primary action (whole card opens the editor, keeps Discard); **Add from rates** fills the real per-size price from `pricing_config` (not the NULL legacy `unit_price`)
- [x] Editable extension on review page — owner sets/edits the extension's **extra days** on the review screen and the extension line **auto-prices** via `resolveExtensionPrice` (extra days × the size's `day_rate`); days > 0 adds the line (or edits it in place), 0 removes it, no `day_rate` shows the needs-rate note; quantity stays = extra days so the paid-extension pickup hook advances by exactly that many; server `recompute-extension` route mirrors `recompute-swap` (rewrites only the extension line, refuses signed/paid). Even a swap-only draft can add an extension here.
- [x] **Pickup/asset Phase 2a — asset registry + availability counted from the fleet.** Two new tables: `assets` (one row per physical dumpster — `size`, owner-assigned `label` unique per business, `status` available/out/at_yard/out_of_service, `notes`, `active`) and `assignments` (the unit↔job link — `asset_id`, `lead_id`, `dropped_at`, `picked_up_at`, `weighed_at`; **created empty and deliberately unused until 2b**). Inventory page is now a **fleet registry** (add/edit/retire units, inline status change, show-retired toggle) over a derived per-size rollup; `GET|POST|PUT|DELETE /api/assets`. Availability's "how many do I own" now counts **registered assets** (`active=1`, `out_of_service` excluded) instead of the typed `inventory_pool.quantity` — `inventoryService.getFleetBySize`; the date-overlap job counting and size normalization are untouched, and availability still never requires a unit to be assigned to a job (`out`/`at_yard` don't affect it). Existing businesses are seeded from their pool counts on boot, so **post-migration availability is byte-identical** to the old pool math (verified: 1000 business × size × window comparisons, including non-zero `units_in_service`, a 0-quantity size, a second tenant, and windows with real booked jobs). `inventory_pool` stays as the per-size registry (row id + notes) and its counts are mirrored from the fleet; its count fields are no longer writable via the API. No drop/pickup wiring, no overage change, swap markers / `units_out` / completion gate untouched. **2b (drop/pickup assignment) and 2c (per-unit overage, swap allowance, size write-back, checkbox double-arm) remain.**
- [x] **Pickup/weight Phase 1** — connected pickup flow + lbs weight + editable dump ticket. Schedule pickup rows are now actionable cards (**Call** `tel:`, **Navigate** Google-Maps directions to the delivery address, **Record pickup** opening the same `DumpTicketAction`, now shared at `components/home_services/DumpTicketAction.jsx`); weight is entered in **pounds** and converted at ONE server boundary (`jobLifecycle.tonsFromLbs`, the only ÷2000) with **tons still the stored unit**; a recorded weight is **editable** and the ticket is the source of truth — `PATCH /leads/:id/dump-ticket/:index` rewrites the ticket, regenerates that ticket's overage invoice line (voids a now-empty invoice, raises a fresh one when a correction becomes chargeable, leaves any swap line untouched) and appends a "weight corrected" activity entry; blocked with a 409 once that ticket's invoice is signed/paid. Swap markers + `units_out` completion gating untouched.

---

## 1. Blocks customer #1 (must-fix before a second business onboards)
- [ ] **Auth/tenant leak (top priority).** `attachBusiness` (auth.js:78-93) never 401s — no/invalid token falls back to Valley Binz (business_id=1). Unauthenticated routes under it: reads `GET /all` (leads.js:307), `GET /`, `GET /:id`, `/:id/activity`, `/:id/customer`; mutations `PUT /:id` (:381), `DELETE /:id` (:1203), `POST /:id/call` (:609), `resend-payment-sms` (:582); plus `upload.js`, `devices.js`, `voice.js` routers and `socket.js:51,71`. (Already guarded: `/manual`, `/:id/dump-ticket`, `/:id/cancel`, `/:id/invoice-review*`.)
- [ ] **New-business onboarding.** Guided setup not built (only OnboardingBanner + Calendly + admin flag, admin.js:140-152). Gate: pricing required before booking. Connect the business's own Twilio number + Stripe Connect (Express).
- [ ] **Default vertical for a new tenant.** `LEADFLOW_DEFAULT_VERTICAL` defaults Twilio leads to `auto_dealer`; a home-services business needs `home_services` (webhook.js:367…).
- [ ] **Env/config prerequisites per tenant:** `JWT_SECRET`, `STRIPE_WEBHOOK_SECRET` + webhook, `STRIPE_CONNECT_WEBHOOK_SECRET` + connected-accounts webhook, hardcoded URLs → env.
- [ ] **Confirm Railway volume mounted/writable** so the fallback-DB path never silently activates.

## 2. Correctness sharp-edges (wrong output for a live business — near-term)
- [ ] **Contracts ship with literal `_____` blank fill-ins** (contractTemplates.js:25-59). Real customers sign these now.
- [ ] **Invoice dedup is per-`lead_id`** → follow-up calls can duplicate invoices. Needs engagement/customer-level dedup.
- [ ] **Cancellation never fires from a call.** Marker read/cleared but never set; producer stub (HANDOFF §10.5).
- [ ] **Owner can't edit a job's delivery address.** No UI field (leads.js:879); writes to `leads.vertical_data.deliveryAddress`.
- [ ] **Auto-correct customer name spelling.** Customer name is written once and never overwritten (`enrichCustomerFromLead`, customerService.js:90-92 only fills blank fields), so an early wrong spelling persists and prints on invoices. Recognize an authoritative spelling (customer spells it at booking) and update the stored name.
- [ ] **Dump-ticket / weight-entry rework.** ~~(a) tons-vs-lbs input default confusion~~ ✅ Phase 1; ~~(b) editing a recorded weight doesn't update the dump-ticket section or the activity feed~~ ✅ Phase 1. ~~(c) swap-out checkbox double-arm~~ ✅ **Phase 2 Build A** — the marker is now checked before the checkbox in `recordDumpTicket`, so a ticked box can never leave `pendingSwapOuts` armed (and the UI hides the box when a paid swap is already pending). The same-class re-arm from paying a swap ticket's own bill is fixed too.

### Pickup rework — remaining phases (Phase 1 shipped; see §0)
- [x] **Phase 2a — asset model foundation.** ✅ Shipped (see §0): `assets` + `assignments` tables, fleet registry UI, availability counted from registered units, existing businesses migrated with verified parity.
- [x] **Phase 2b — drop/pickup assignment.** ✅ Shipped (see §0): required unit selection at drop and at pickup, `assignments` written, asset status driven off those events, assigned unit surfaced on the schedule + customer profile. Capture only — overage/weight/swap/completion unchanged.
- [x] **Phase 2c — per-unit overage attribution + size write-back.** ✅ Shipped (see §0): the weight belongs to an assignment, so the overage prices on the unit's own size and `units_out` / completion run on that unit's job; yard queue for weighing cans later; size write-back on a size-changing drop; two-concurrent-jobs bug closed. Swap markers, `units_out` accounting and the completion gate unchanged.
- [x] **Phase 2d — configurable swap weight allowance.** ✅ Shipped as **Build B** (see §0): per-business `swapWeightAllowance` (`full` default / `none`) on the Pricing page; the replacement haul is identified by its assignment not being the job's first (gated on the job having actually swapped) and that decision is stored on the ticket so corrections re-price on the same basis; applied at the single `computeOverage({ replacementHaul })` boundary. A *custom* allowance amount was not built — full/none only. **Pickup rework Phase 2 is complete.**
- [x] **Phase 3 — weight photo → OCR.** ✅ Shipped (see §0), as a *shortcut* rather than a co-equal path: the lbs box is always visible and typeable, and the camera beside it pre-fills that same box from `services/scaleTicketReader.js` (a new file copying the vision pattern, not a change to `extractionEngine.js`), naming the ticket line it read so the owner can check it. Nothing auto-submits and a failed read never blocks manual entry. The photo is kept on the persistent volume as overage evidence. **Deliberately not done**: the ticket is still written with `source:'manual'` — the weight the server records is the one the owner confirmed, not the one the model read, so calling it `'ocr'` would misattribute it. Add a separate `readSource` field if the provenance is ever wanted.
- [ ] **Confirm the scale-ticket auto-read against real tickets in production.** The live vision round-trip is unverified locally (no `ANTHROPIC_API_KEY` on the dev machine) — the parser and the failure path are covered by tests, the model call itself first runs in Railway. Check it against a few real Valley Binz tickets (gross/tare/net layouts, tons vs lbs) and tune the prompt if it reads the wrong line.
- [ ] **Scale-ticket photos are never purged.** They accumulate on the Railway volume alongside recordings; `recordingCleanup` only deletes Twilio-hosted recordings and doesn't sweep the directory. Decide a retention (they're overage evidence, so probably longer than 30 days) before the volume fills.
- [ ] **Phase 4 — driver role.** Create-driver endpoint + a `requireRole` guard. ⚠️ Today a driver token would have **full owner API access** — role exists on the user row but nothing enforces it.
- [x] **Phase 5 — dump sites.** ✅ Shipped (see §0): `dump_sites` table, `/api/dump-sites` CRUD, a Dump Sites page in the nav, and the site picker + maps deep-link inside the guided pickup flow. Names and addresses only — no geocoding, no distance, no mileage-fee wiring. The chosen site is recorded on the dump ticket and in the job timeline.
- [ ] **Later — SMS "on my way"** from the pickup card (deliberately omitted from Phase 1; ships with the SMS/A2P phase).
- [ ] **Verify legacy `/pay` state.** Harvest says `/pay/:leadId` page + `sendPaymentLinkEmail` may still exist as a disabled stub and booking may still email the legacy link. Finish removal → `/invoice/:token`.

## 3. Known functional gaps (needed, not blocking)
- [ ] **Concurrent multi-job** — one open job per customer; multi-site contractors unsupported (structural) + multi-job pricing override + "which job is this call about?"
- [ ] **Delivery-vs-pickup date-change classification** — never built.
- [ ] **Swap availability/inventory** — replacement invisible to availability pool.
- [ ] ~~**Weight display**~~ ✅ Phase 1 (tickets read in lbs + tons, editable, on the profile AND the schedule pickup card). **Driver weight-entry** still pending → pickup Phase 4.
- [ ] **Overage config dual-source cleanup** (pricing_config vs legacy settings).
- [ ] **Inquiry auto-book backfill** — deferred.
- [ ] **Single pending-change/draft limit** — 2nd pending change per job is skipped.

## 4. Features not started (bigger builds)
- [ ] **Dispatch** — scope TBD.
- [ ] **Driver / limited-user view** — iOS role reserved, no UI (AuthManager.swift:40-46). → pickup **Phase 4**.
- [ ] **Mileage / distance pricing + geocoding/mapping** — configurable but never computed (pricingService.js:312-313).
- [ ] **Per-business contract builder** — inert seam (invoiceService.js:767-768); no Settings editor.
- [ ] **Full Insights/analytics page** — stub over morning brief (InsightsPage.jsx:50-54).
- [ ] **Photo-OCR dump tickets** — only `source:'manual'` wired. → pickup **Phase 3**.
- [ ] **Dump sites** — no table/CRUD; nowhere to record where a load went. → pickup **Phase 5**.
- [ ] **A2P 10DLC + payment SMS** — compliance pages only; SMS paused.
- [ ] **In-app voicemail access (high owner-impact)** — calls forward to the app, bypassing native iPhone voicemail; voicemails are only listenable on the web app. Add in-app playback + a new-voicemail notification; verify whether native carrier voicemail can be restored.
- [ ] **Future verticals** — insurance_agent + HVAC (extraction only).

## 5. iOS / infra
- [ ] **iOS incoming-call ring bug** — memory says no ring / no CallKit screen (APNs sandbox↔prod suspected). Verify — recent live calls worked, may be stale.
- [ ] **In-app VoIP token endpoint 503s** until Twilio SIDs set (voice.js:28-42).
- [ ] **Outbound dial** — point Twilio TwiML App Voice URL at `/api/voice/outbound`; keep caller ID +18155030701 Verified.
- [ ] **iOS JWT auth** — verify current attachment state.
- [ ] **LeadFlow→Stream rebrand in iOS** — bundle id/types still "LeadFlow." Cosmetic.
- [ ] **APNs prod toggle** — `APNS_PRODUCTION` (apns.js:5).

## 6. Cleanup / dead code / stale docs
- [ ] Delete dead client code: `LeadDetailPage.jsx`, `LeadCardExpanded.jsx`, `ConfidenceMeter.jsx`; unused `sendPaymentSms` import (webhook.js:12).
- [ ] Legacy auto-dealer UI behind the "Auto Dealer" tab — vestigial, still reachable.
- [ ] Vestigial schema fields (assigned_dumpster_id, needs_dumpster_assignment, duplicate ALTER, retired mid-states).
- [ ] Stale comments: pricingService.js:15/:89 "Prompt B not wired" (overage IS wired).
- [ ] Rewrite or delete stale `README.md`.

## 7. Already built / by-design — DO NOT chase
- Overage pricing IS wired (jobLifecycle.js:205-235). Online card payment IS live (Stripe Connect/publicInvoices). Inventory is intentionally pool-based. Multi-job = read-time engagements. Call-intent Part 1/2 + swap fixes shipped.
