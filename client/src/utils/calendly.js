// Single source of truth for the Calendly setup-call scheduler. Shared by the
// post-signup step (inline embed) and the dashboard onboarding banner (link) so
// the booking URL can never drift between the two.
export const CALENDLY_URL = 'https://calendly.com/threetscapital/30min';
